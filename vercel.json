{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm --prefix apps/web ci && npm --prefix apps/web run build && rm -rf public && cp -r apps/web/dist public",
  "functions": {
    "apps/api/main.py": {
      "maxDuration": 60
    }
  }
}
